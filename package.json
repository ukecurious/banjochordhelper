{
  "name": "banjochordhelper",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://ukecurious.github.io/banjochordhelper",
  "scripts": {
    "build": "esbuild src/index.jsx --bundle --platform=browser --minify --outfile=public/js/index.js",
    "start": "npm run build && live-server public",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d public"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "gh-pages": "^5.0.0",
    "live-server": "^1.2.0"
  }
}
